# API Reference

Start with `TramoSeatsConfig` and `adjust_dataframe` for multi-series
TRAMO/SEATS workflows. `DataFrameAdjustmentResult` exposes component
DataFrames, forecasts, and per-series fitted results. The objects below form
the supported top-level Python API; X13 and file-oriented APIs are included as
secondary interfaces.

```{eval-rst}
.. automodule:: demetrapy
	:members:
	:undoc-members:
	:show-inheritance:
```